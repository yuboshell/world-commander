.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/
*.aux
*.log
*.out

# Downloaded reference papers — kept local (heavy binaries); cited via LITERATURE.md + arXiv
papers/*.pdf

# Obsidian vault config and local workspace state (browsed locally, not shared)
.obsidian/

# Web clips (Obsidian Web Clipper) — a local reading inbox. Distil each clip into a
# curated node (LITERATURE.md here, or a memex concept note) and commit that, not the dump.
raw/*
!raw/README.md
